import React from 'react';
import FlatButton from 'material-ui/FlatButton';

const CMButton = () => (
  <FlatButton
    name="CMButton"
    className="cm-btn-default"
  />
);

export default CMButton;
